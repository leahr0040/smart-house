---
phase: 01-schema-data-conventions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
autonomous: false
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "prisma migrate status shows the rename migration applied; the live table is named users (not User) and retains every pre-existing auth row"
    - "npm run build compiles and npm test passes the existing root/auth regression after the rename"
    - "deleted_at (nullable) exists on the users table for soft delete (DATA-03)"
  artifacts:
    - "prisma/migrations/<ts>_rename_user_to_users/migration.sql containing RENAME TABLE (no DROP+CREATE)"
    - "User model in prisma/schema.prisma carrying @@map(\"users\") and deletedAt @map(\"deleted_at\")"
  key_links:
    - "User model @@map(\"users\") resolves to the renamed live table so existing prisma.user.* auth queries keep working unchanged"
---

<objective>
Rename the existing, populated `User` table to `users` via a hand-authored `RENAME TABLE` migration (never DROP+CREATE), and add the `deleted_at` soft-delete column to it — delivering the existing-table half of DATA-01 (snake_case `@@map` on the last un-mapped model) and the User portion of DATA-03 (soft delete). This is the smallest, most isolated, and highest-data-loss-risk migration of Phase 1, so it ships first and alone. `RefreshToken` already carries `@@map("refresh_tokens")` and the live table is already correctly named (verified against `prisma/migrations/20260617121303_add_refresh_tokens/migration.sql`) — no rename needed there.

Per D-11 the `id` type widen (Int→BigInt) is deliberately deferred to plan 01-02 so each risky, hand-verified migration is a distinct reviewable diff. This plan touches only the table name and the additive `deleted_at` column; the `id` stays `Int` here.

Purpose: Preserve all existing auth data while applying the snake_case table-name convention uniformly.
Output: One hand-verified rename migration applied to the live DB; `users` table live; build + existing tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-data-conventions/01-RESEARCH.md
@.planning/phases/01-schema-data-conventions/01-CONTEXT.md
@.planning/phases/01-schema-data-conventions/01-PATTERNS.md
@prisma/schema.prisma
@prisma/migrations/20260616152601_init/migration.sql
@prisma/migrations/20260617121303_add_refresh_tokens/migration.sql
</context>

<artifacts_this_phase_produces>
This plan (01-01) produces:
- Modified `User` model in `prisma/schema.prisma`: adds `@@map("users")` and `deletedAt DateTime? @map("deleted_at")`. The `id` remains `Int` in this plan (widened in 01-02).
- New migration directory `prisma/migrations/<timestamp>_rename_user_to_users/migration.sql` — hand-authored to emit `ALTER TABLE ... RENAME TO ...` plus `ALTER TABLE ... ADD COLUMN deleted_at`, never a DROP/CREATE pair.
- Regenerated Prisma client under `src/generated/prisma/` reflecting the `@@map` and `deletedAt` field.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Map User to users, add deleted_at, and hand-author the rename migration SQL</name>
  <files>prisma/schema.prisma, prisma/migrations/&lt;timestamp&gt;_rename_user_to_users/migration.sql</files>
  <read_first>
    - prisma/schema.prisma (current User model, lines 15-23 — no @@map today; RefreshToken model lines 25-36 for the snake_case convention to copy)
    - prisma/migrations/20260616152601_init/migration.sql (the exact current `User` table shape the rename must preserve — column names, types, `User_email_key` unique index, charset utf8mb4/utf8mb4_unicode_ci)
    - .planning/phases/01-schema-data-conventions/01-RESEARCH.md (Pattern 1: Hand-Authored Rename Migration)
    - .planning/phases/01-schema-data-conventions/01-PATTERNS.md (the "Snake_case mapping convention" and rename analog sections)
  </read_first>
  <action>
    In prisma/schema.prisma, on the existing `User` model add `@@map("users")` (matching the RefreshToken `@@map` style) and a new field `deletedAt DateTime? @map("deleted_at")` (DATA-03, per D-11 soft delete). Leave `id` as `Int @id @default(autoincrement())` for this plan — the Int→BigInt widen is 01-02's job. Do NOT touch RefreshToken (already `@@map("refresh_tokens")`, already correctly named live).

    Regenerate the client with `npm run prisma:generate` so `src/generated/prisma/` reflects the map + new field.

    Generate the migration draft WITHOUT applying: run `npx prisma migrate dev --name rename_user_to_users --create-only`. Prisma does not detect renames — its draft will emit a destructive `DROP TABLE`/`CREATE TABLE` pair for the name change. Hand-edit the generated `migration.sql` so the entire file is exactly two statements: an `ALTER TABLE ... RENAME TO ...` renaming `User` to `users`, followed by an `ALTER TABLE ... ADD COLUMN deleted_at` adding the nullable `DATETIME(3)` column to `users`. Remove every `DROP TABLE` and `CREATE TABLE` statement Prisma generated. Preserve the existing `User_email_key` unique index implicitly via the rename (renaming the table keeps its indexes). Reference the literal current column definitions from `20260616152601_init/migration.sql` when confirming nothing else must change.

    Do NOT apply yet — application happens in Task 3 after the human data-loss gate in Task 2.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
    <automated>grep -Eic 'rename to' prisma/migrations/*_rename_user_to_users/migration.sql</automated>
    <automated>test $(grep -Eic 'drop table|create table' prisma/migrations/*_rename_user_to_users/migration.sql) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exits 0 (schema parses with @@map + deletedAt).
    - The rename migration.sql contains an `ALTER TABLE ... RENAME TO` statement (grep count >= 1).
    - The rename migration.sql contains ZERO `DROP TABLE` and ZERO `CREATE TABLE` statements (grep count == 0).
    - `deleted_at` is added as a nullable `DATETIME(3)` column (grep confirms `ADD COLUMN` + `deleted_at`).
    - schema.prisma User model shows `@@map("users")` and `deletedAt DateTime? @map("deleted_at")`; `id` is still `Int`.
  </acceptance_criteria>
  <done>User is mapped to `users` with a `deleted_at` column in schema; a hand-authored rename migration exists that renames (not recreates) the table; nothing applied to the DB yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Human data-loss gate — verify the rename SQL preserves existing auth data</name>
  <what-built>A hand-authored `prisma/migrations/<timestamp>_rename_user_to_users/migration.sql` that renames the live, populated `User` table to `users` and adds a nullable `deleted_at` column, without dropping or recreating the table.</what-built>
  <how-to-verify>
    1. Open the generated file `prisma/migrations/<timestamp>_rename_user_to_users/migration.sql`.
    2. Confirm it contains an `ALTER TABLE ... RENAME TO ...` statement renaming `User` to `users`.
    3. Confirm there is NO `DROP TABLE` and NO `CREATE TABLE` anywhere in the file (a DROP+CREATE pair would delete every existing user + refresh-token relationship — the exact data-loss this gate exists to prevent, threat T-01-01).
    4. Confirm the only other statement is an `ALTER TABLE users ADD COLUMN deleted_at DATETIME(3) NULL` (or equivalent) — additive, nullable, safe.
    5. This is the one destructive-adjacent operation in this plan; it is not auto-approvable.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to apply, or describe the SQL issue to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply the rename migration and confirm build + existing tests green</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criterion 1 and 6 — the acceptance bar)
    - .planning/phases/01-schema-data-conventions/01-RESEARCH.md (Validation Architecture — the compile + migration smoke-check for this schema/infra phase; no red test)
  </read_first>
  <action>
    Apply the human-approved rename migration to the live MariaDB instance with `npx prisma migrate dev` (this applies the pending edited migration and regenerates the client; it must NOT generate a second migration — if it does, the hand-edited SQL did not match the schema and must be reconciled before continuing). This is the [BLOCKING] DB-apply step schema-gate requires — it must run after the schema change and before verification, because build/type checks pass off the generated client even when the DB is not yet migrated (a false-positive state).

    Then run the phase acceptance smoke-check: `npm run build` (tsc, zero errors) and `npm test` (existing `root.test.ts` regression). Per PROJECT.md's schema/infra exception there is no red test — the acceptance bar is compile + migration + existing tests green.
  </action>
  <verify>
    <automated>npx prisma migrate status</automated>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate status` reports the schema up to date with the rename migration applied (exit 0, no pending migrations, no drift).
    - `npm run build` exits 0 with zero TypeScript errors.
    - `npm test` passes (existing root regression green against the renamed `users` table).
    - The live table is named `users`; existing auth rows are preserved (row count unchanged from before the migration).
  </acceptance_criteria>
  <done>The `users` table is live with all prior data intact and a `deleted_at` column; build and existing tests pass; DATA-01 (rename half) and DATA-03 (User soft-delete column) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma migration engine → live `User` table | The migration DDL crosses into a populated production-shape table holding existing auth accounts and refresh-token relationships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Repudiation | rename migration.sql (`User`→`users`) | high | mitigate | Hand-author the SQL as `RENAME TABLE`, negative-grep for DROP/CREATE, and gate apply behind a blocking-human review (Task 2) — a DROP+CREATE would silently destroy every existing user row |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs occur in Phase 1; `nanoid`/`uuid` install + legitimacy gate are deferred to Phase 2/5 per RESEARCH Package Legitimacy Audit. No install task exists in this plan |
</threat_model>

<verification>
- Rename migration applied cleanly; `users` live with prior rows intact.
- `npm run build` and `npm test` green.
- schema.prisma User model: `@@map("users")` + `deletedAt @map("deleted_at")`, `id` still `Int`.
- No DROP/CREATE in the rename migration SQL.
</verification>

<success_criteria>
DATA-01 (existing-table rename via hand-authored `ALTER TABLE ... RENAME`) and the User portion of DATA-03 (`deleted_at`) are satisfied with zero data loss; the tree is green and ready for the Int→BigInt widen in 01-02.
</success_criteria>

<output>
Create `.planning/phases/01-schema-data-conventions/01-01-SUMMARY.md` when done.
</output>
