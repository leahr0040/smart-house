---
phase: 02-entity-crud-multi-tenancy
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/routes/rooms/index.ts
  - src/routes/rooms/schemas.ts
  - src/services/room.ts
  - src/test/routes/rooms.test.ts
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-03, ROOM-04, TEST-01, TEST-05, TEST-08]
user_setup: []

must_haves:
  truths:
    - "An authenticated user can create a room under a house they own (POST /houses/:housePublicId/rooms), list rooms in that house (GET /houses/:housePublicId/rooms), view (GET /rooms/:roomPublicId), update (PATCH /rooms/:roomPublicId), and soft-delete (DELETE /rooms/:roomPublicId) their own rooms"
    - "Creating a room under a house owned by a different user returns 404 (parent-ownership bypass blocked, HOUSE-05 spirit)"
    - "GET/PATCH/DELETE /rooms/:roomPublicId for a room owned by another user returns 404 (not 403)"
    - "Every /rooms and /houses/:housePublicId/rooms route returns 401 without a valid JWT (TEST-08)"
    - "Deleting a room soft-deletes the room AND all its devices in one transaction; soft-deleted rooms are excluded from list reads (ROOM-04 + TEST-05)"
    - "Responses expose publicId + safe metadata (name, floor, roomType) only; no internal BigInt id (D-03)"
  artifacts:
    - src/routes/rooms/index.ts
    - src/routes/rooms/schemas.ts
    - src/services/room.ts
    - src/test/routes/rooms.test.ts
  key_links:
    - "src/routes/rooms/index.ts exports prefixOverride = '' and declares absolute paths so POST /houses/:housePublicId/rooms and /rooms/:roomPublicId register correctly under autoload"
    - "createRoom verifies parent house ownership via tx.house.findFirst({ where: { publicId: housePublicId, userId } }) BEFORE inserting the room — nested creation cannot bypass ownership"
    - "deleteRoom cascade uses tx.device.updateMany + tx.room.updateMany directly (never deleteMany) — transaction-safe (Pitfall 1)"
  prohibitions:
    - "No response includes an internal numeric id — construct responses field-by-field with publicId only"
    - "No re-parenting — house_id is fixed at creation; PATCH touches name/floor/roomType only (D-04/D-05)"
    - "Cascade delete must NOT call tx.device.deleteMany inside the transaction (Pitfall 1) — use direct updateMany"
    - "The route file must NOT rely on autoload directory prefixing — it exports prefixOverride = ''"
---

## Phase Goal

**As an** authenticated user, **I want to** create, view, update, and soft-delete rooms inside houses I own, **so that** I can organize my home into rooms without any other user reaching them.

<objective>
Deliver the full Room CRUD vertical slice end-to-end: nested creation under a parent house (with parent-ownership verification), TypeBox-validated routes → ownership-embedded service → room→devices cascade soft-delete → multi-tenancy (404) and auth (401), with the DB-touching test suite.

Purpose: Rooms sit between houses and devices. The distinctive risk here is nested-parent ownership bypass — a user must not create a room under someone else's house. This slice locks that boundary.
Output: `src/routes/rooms/{index,schemas}.ts`, `src/services/room.ts`, `src/test/routes/rooms.test.ts`.
Sequence: CLAUDE.md structure-first → tests-red → implement (three tasks, three atomic commits).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-entity-crud-multi-tenancy/02-CONTEXT.md
@.planning/phases/02-entity-crud-multi-tenancy/02-RESEARCH.md
@.planning/phases/02-entity-crud-multi-tenancy/02-PATTERNS.md
@src/routes/auth/index.ts
@src/services/user.ts
@src/lib/prisma.ts
@prisma/schema.prisma
</context>

## Artifacts This Phase Produces

New symbols/files created by THIS plan (source-grounding: exclude from drift verification):
- `src/services/room.ts` — `createRoom(input)` (parent-ownership check + houseId resolve), `listRooms(housePublicId, userId)`, `getRoom(roomPublicId, userId)`, `updateRoom(roomPublicId, userId, patch)`, `deleteRoom(roomPublicId, userId)` (transactional cascade to devices)
- `src/routes/rooms/schemas.ts` — `CreateRoomSchema`, `PatchRoomSchema` (TypeBox, `additionalProperties:false`), Static types
- `src/routes/rooms/index.ts` — `FastifyPluginAsyncTypebox` plugin; exports `prefixOverride = ''`; routes POST/GET `/houses/:housePublicId/rooms`, GET/PATCH/DELETE `/rooms/:roomPublicId`
- `src/test/routes/rooms.test.ts` — integration tests

Consumes from 02-01 (do NOT recreate): `prisma` extended client, `createTwoTestUsers`, `seedHouse`, `seedDevice` from `src/test/helpers/fixtures.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold — TypeBox schemas, room service signatures, route stubs</name>
  <files>src/routes/rooms/schemas.ts, src/services/room.ts, src/routes/rooms/index.ts</files>
  <read_first>
    - src/routes/auth/index.ts (plugin + preHandler + explicit response construction; do NOT copy the Number(user.id) anti-pattern)
    - src/services/user.ts (service CRUD shape)
    - .planning/phases/02-entity-crud-multi-tenancy/02-PATTERNS.md §schemas (CreateRoomSchema example), §services, §Shared Patterns
    - .planning/phases/02-entity-crud-multi-tenancy/02-RESEARCH.md §Pattern 1, §Code Examples (PATCH partial), §Pitfall 4
    - prisma/schema.prisma (Room: houseId, userId denormalized, name, floor default 0, roomType nullable)
  </read_first>
  <action>
    Create src/routes/rooms/schemas.ts with TypeBox: CreateRoomSchema = Type.Object({ name: Type.String({ minLength:1, maxLength:191 }), floor: Type.Optional(Type.Integer()), roomType: Type.Optional(Type.String({ maxLength:191 })) }, { additionalProperties:false }); PatchRoomSchema = Type.Partial of the object form (name, floor, roomType), additionalProperties:false; export Static body types. Create src/services/room.ts importing prisma with typed // TODO signatures: createRoom({ housePublicId, userId, name, floor?, roomType? }), listRooms(housePublicId, userId), getRoom(roomPublicId, userId), updateRoom(roomPublicId, userId, patch), deleteRoom(roomPublicId, userId). Create src/routes/rooms/index.ts typed FastifyPluginAsyncTypebox, exporting `export const prefixOverride = ''`, declaring absolute paths: POST /houses/:housePublicId/rooms, GET /houses/:housePublicId/rooms, GET /rooms/:roomPublicId, PATCH /rooms/:roomPublicId, DELETE /rooms/:roomPublicId — each with preHandler: fastify.authenticate and the matching schema; handler bodies call the (TODO) services and build responses field-by-field. Compile-clean with minimal/TODO bodies.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - src/routes/rooms/index.ts exports `prefixOverride` equal to an empty string and declares POST /houses/:housePublicId/rooms plus /rooms/:roomPublicId routes
    - schemas.ts uses TypeBox with additionalProperties:false
    - room.ts exports createRoom, listRooms, getRoom, updateRoom, deleteRoom
  </acceptance_criteria>
  <done>Room schemas, service signatures, and route stubs compile at the correct nested + by-id absolute paths.</done>
</task>

<task type="auto">
  <name>Task 2: Tests (red) — nested create, parent-ownership 404, cross-tenant 404, 401, soft-delete, cascade</name>
  <files>src/test/routes/rooms.test.ts</files>
  <read_first>
    - src/test/routes/root.test.ts (build(t) + app.inject convention)
    - src/test/helpers/fixtures.ts (createTwoTestUsers, seedHouse, seedDevice — from 02-01)
    - .planning/phases/02-entity-crud-multi-tenancy/02-PATTERNS.md §test shape
  </read_first>
  <action>
    Create src/test/routes/rooms.test.ts using build(t) + createTwoTestUsers(app). Set up a parent house for userA via seedHouse(userA.userId). Cover: (1) POST /houses/:housePublicId/rooms (userA) → 201/200, body has publicId + name + floor + roomType, no id key; (2) GET /houses/:housePublicId/rooms returns userA's rooms as a bare array; (3) GET /rooms/:roomPublicId returns the room for the owner; (4) PATCH /rooms/:roomPublicId updates name/floor/roomType; (5) DELETE /rooms/:roomPublicId soft-deletes — GET list omits it, GET by id → 404; (6) parent-ownership bypass: userB POST /houses/:housePublicId/rooms against userA's house → 404 (ROOM-01 ownership); (7) cross-tenant: userB GET/PATCH/DELETE on userA's room → 404 (TEST-01); (8) auth: every route with no token → 401 (TEST-08); (9) soft-delete exclusion (TEST-05); (10) cascade (ROOM-04): seed two devices under userA's room via seedDevice, DELETE the room, assert via direct prisma reads that the devices are now soft-deleted (deletedAt set) and excluded from ownership reads while the room itself is soft-deleted. Build then run — expect RED.
  </action>
  <verify>
    <automated>npm run build && node --test dist/src/test/routes/rooms.test.js; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - rooms.test.ts compiles (build exits 0)
    - The file RUNS and FAILS on assertions (verify asserts non-zero test exit)
    - Tests include a parent-ownership-bypass 404 (userB creating under userA's house), a cross-tenant 404, a 401 for every route, a soft-delete-exclusion case, and a room→devices cascade case
    - The create-response test asserts `publicId` present and `id` absent
  </acceptance_criteria>
  <done>Full room test suite written and running red against TODO handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Implement — parent-ownership create + cascade + handlers to green</name>
  <files>src/services/room.ts, src/routes/rooms/index.ts</files>
  <read_first>
    - .planning/phases/02-entity-crud-multi-tenancy/02-PATTERNS.md §services (SAFE cascade, ownership query), §RESEARCH.md Pitfall 1, Pattern 2
    - src/lib/prisma.ts (readGuard, create hook mints publicId)
  </read_first>
  <action>
    Fill src/services/room.ts. createRoom → prisma.$transaction(async tx => { const house = await tx.house.findFirst({ where: { publicId: input.housePublicId, userId: input.userId }, select: { id: true } }); if (!house) return null; return tx.room.create({ data: { userId: input.userId, houseId: house.id, name: input.name, floor: input.floor ?? undefined, roomType: input.roomType ?? null } }) }) — verify parent house ownership BEFORE insert so a user cannot create under another user's house; publicId minted by the hook (tx-safe). listRooms → resolve house ownership via findFirst({ where: { publicId: housePublicId, userId }, select: { id: true } }); if null return null (route → 404); else prisma.room.findMany({ where: { houseId: house.id, userId } }). getRoom → prisma.room.findFirst({ where: { publicId: roomPublicId, userId } }). updateRoom → resolve ownership via findFirst({ where: { publicId, userId }, select: { id: true } }); if null return null; else prisma.room.update({ where: { id }, data }) destructuring only name/floor/roomType (never house_id — no re-parenting, D-05; never spread the raw body). deleteRoom → prisma.$transaction(async tx => { const room = await tx.room.findFirst({ where: { publicId: roomPublicId, userId }, select: { id: true } }); if (!room) return null; const now = new Date(); await tx.device.updateMany({ where: { roomId: room.id }, data: { deletedAt: now } }); await tx.room.updateMany({ where: { id: room.id }, data: { deletedAt: now } }); return room }) — direct updateMany only. In src/routes/rooms/index.ts wire handlers: userId = BigInt(request.user.id); null service result → fastify.httpErrors.notFound(); construct responses field-by-field (publicId, name, floor, roomType, createdAt/updatedAt) — never include id, never raw-row spread; lists return a bare mapped array. Run the room test file to green.
  </action>
  <verify>
    <automated>npm run build && node --test dist/src/test/routes/rooms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test dist/src/test/routes/rooms.test.js` exits 0
    - createRoom resolves parent house via findFirst with { publicId, userId } before creating the room
    - deleteRoom uses tx.device.updateMany + tx.room.updateMany (grep confirms no `deleteMany` inside src/services/room.ts)
    - updateRoom writes only name/floor/roomType (no houseId in the update data)
    - Cross-tenant and parent-ownership-bypass paths return 404; no-token → 401; responses carry publicId and no id
  </acceptance_criteria>
  <done>Room CRUD + nested-create ownership + room→devices cascade + multi-tenancy + auth all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /rooms + /houses/:housePublicId/rooms API | Untrusted JWT + params/body; both the room AND its parent house ownership must be enforced |
| service → DB | relationMode="prisma": app layer is the sole ownership + cascade enforcement point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-R01 | Elevation of Privilege | POST /houses/:housePublicId/rooms (nested-parent ownership bypass) | high | mitigate | createRoom resolves parent house via findFirst {publicId,userId} before insert; foreign house → 404 (test asserts) |
| T-2-R02 | Information Disclosure / EoP | GET/PATCH/DELETE /rooms/:roomPublicId (IDOR / BOLA) | high | mitigate | Ownership-embedded where:{publicId,userId}; cross-tenant → 404 not 403 (TEST-01) |
| T-2-R03 | Spoofing | All room routes without JWT | high | mitigate | preHandler: fastify.authenticate on every route; 401 asserted (TEST-08) |
| T-2-R04 | Tampering | PATCH re-parenting / mass assignment (house_id, userId, deletedAt) | medium | mitigate | Schema declares only name/floor/roomType + additionalProperties:false; service destructures those three; no house_id in update (D-05) |
| T-2-R05 | Tampering | Non-atomic room→devices cascade | high | mitigate | Cascade in one $transaction using direct updateMany (Pitfall 1); cascade test asserts devices soft-deleted |
| T-2-R06 | Information Disclosure | Internal BigInt id leakage | medium | mitigate | Responses built field-by-field with publicId only; test asserts no `id` key |
</threat_model>

<verification>
- `npm run build` exits 0
- `node --test dist/src/test/routes/rooms.test.js` exits 0 (full room slice green)
- Parent-ownership bypass → 404, cross-tenant → 404, no-token → 401, room→devices cascade atomic
- No `id` field in room responses; no `deleteMany` inside room.ts cascade; no houseId in updateRoom data
</verification>

<success_criteria>
- ROOM-01..04 satisfied: create-under-owned-house / list / update / soft-delete
- TEST-01 (second-user 404, incl. parent-ownership bypass), TEST-05, TEST-08 covered
- Room→devices cascade atomic and verified
</success_criteria>

<output>
Create `.planning/phases/02-entity-crud-multi-tenancy/02-03-SUMMARY.md` when done.
</output>
